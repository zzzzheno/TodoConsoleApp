## 最低完成標準

你的 Console Todo List 至少要做到這些成果：

### 1. 主選單

啟動後要看到清楚的功能選單，例如：

```text
=== Todo List ===
1. 顯示任務清單
2. 新增任務
3. 標記完成
4. 刪除任務
0. 離開
請選擇功能：
```

使用者可以一直操作，直到選擇離開。

---

### 2. 新增任務

使用者可以輸入一筆待辦事項。

例如：

```text
請輸入任務內容：讀 C# 書
任務已新增
```

要求：

* 空白內容不能新增
* 新增後任務要出現在清單中

---

### 3. 顯示任務清單

要能列出所有任務。

例如：

```text
1. [未完成] 讀 C# 書
2. [完成] 做 Todo List
3. [未完成] 練習 Git
```

要求：

* 每筆任務要有編號
* 每筆任務要顯示完成狀態
* 沒有任務時要顯示「目前沒有任務」

---

### 4. 標記任務完成

使用者可以選擇某個任務，將它改成完成。

例如：

```text
請輸入要標記完成的任務編號：2
任務已標記為完成
```

要求：

* 只能選存在的編號
* 輸入錯誤時不能讓程式當掉
* 已完成的任務要在清單中看得出來

---

### 5. 刪除任務

使用者可以刪除指定任務。

例如：

```text
請輸入要刪除的任務編號：1
任務已刪除
```

要求：

* 只能刪除存在的任務
* 輸入錯誤時不能讓程式當掉
* 刪除後清單要正確更新

---

### 6. 離開程式

使用者選擇離開時，程式要正常結束。

例如：

```text
程式結束
```

---

## 建議加分功能

最低標準完成後，再加這些功能。

### 7. 修改任務內容

可以選擇一筆任務並修改文字。

例如：

```text
原本任務：讀 C# 書
請輸入新內容：讀 C# 物件導向
任務已更新
```

---

### 8. 取消完成狀態

除了標記完成，也可以把完成改回未完成。

例如：

```text
1. [完成] 做 Todo List
請輸入要改回未完成的任務編號：1
任務已改為未完成
```

---

### 9. 顯示統計資訊

例如：

```text
總任務數：5
已完成：2
未完成：3
完成率：40%
```

---

### 10. 篩選任務

可以只顯示：

* 全部任務
* 已完成任務
* 未完成任務

---

## 進階成果：資料保存

等基本功能都完成後，建議做到：

### 11. 關掉程式後資料不消失

也就是：

* 新增任務後，關閉程式
* 重新開啟程式
* 任務還在

這一版可以用檔案保存，不需要資料庫。

成果標準：

```text
第一次執行：
1. [未完成] 學 C#

關掉程式後重新執行：
1. [未完成] 學 C#
```

這是很重要的加分功能。

---

## 不需要做到的東西

這個階段先不要做：

* 登入系統
* 多使用者
* 網頁版
* 資料庫
* 漂亮 UI
* 權限管理
* 雲端同步
* 太複雜的架構

這些不是初版 Todo List 的重點。

---

## 你最後應該交出的成果

你的 Todo List 最終至少應該長這樣：

```text
=== Todo List ===
1. 顯示全部任務
2. 新增任務
3. 修改任務
4. 標記完成 / 未完成
5. 刪除任務
6. 顯示統計
0. 離開
請選擇功能：
```

並且符合這些條件：

```text
可以新增任務
可以查看任務
可以修改任務
可以刪除任務
可以切換完成狀態
輸入錯誤不會當掉
關掉程式後資料還在
```

## 完成判定

你可以用這份清單檢查：

```text
[x] 有主選單
[x] 可以新增任務
[x] 可以顯示任務清單
[x] 可以標記完成
[x] 可以刪除任務
[ ] 可以修改任務
[ ] 可以切換完成 / 未完成
[ ] 可以顯示統計資訊
[x] 錯誤輸入不會讓程式崩潰
[ ] 關掉後資料仍然保留
```